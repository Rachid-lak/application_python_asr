pipeline{ 
agent none   
stages{
  
   /* stage('SonarQube Analysis') {
     agent any
       steps{
      
       withSonarQubeEnv( 'sonarqube') {
       sh "${SonarQubeScanner}/var/jenkins_home/sonar-scanner"
          }
        }
        }*/

stage('SonarQube Analysis') {
            steps {
                script {
                    def scannerHome = tool 'SonarQubeScanner'
                    withSonarQubeEnv() {
                        sh "${scannerHome}/bin/sonar-scanner"
                    }
                }
            }
        }

   }
    
}
