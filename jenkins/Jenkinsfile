pipeline{ 
agent none   
stages{
  
    stage('SonarQube Analysis') {
     agent any
       steps{
      
       withSonarQubeEnv( 'SonarQubeScanner') {
       sh "${scannerHome}/bin/sonar-scanner"
          }
        }
        }
   }
    
}
